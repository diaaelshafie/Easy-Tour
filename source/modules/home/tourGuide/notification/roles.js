import { systemRoles } from '../../../../utilities/systemRoles.js'

export const notificationRoles = {
    receiveNotification: [systemRoles.tourGuide],
    openNotification: [systemRoles.tourGuide]
}